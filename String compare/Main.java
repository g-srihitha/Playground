#include<iostream>
#include<string.h>
using namespace std;
int main()
{
  string a,b;
  cin>>a>>b;
  if(!a.compare(b))
    cout<<"Strings are same";
  else
    cout<<"Strings are not same";
  //Type your code here.
}